<%- include('partials/header') %>                <!--to render header page using ejs-->

<h1>Home</h1>
<p> <%= homeStartingContent %> </p> 


<%  Blog.forEach(doc => { %>
    <h1> <%=doc.title %> </h1>
    <p> <%=doc.content.substring(0, 99) + " ... " %> <a href="/post/<%=doc.title%>">Read More</a> </p>    
<% }); %>


<% /* %>
<% for(var i = 0; i<pos.length; i++){    %>

    <h1> <%=pos[i].title%> </h1>
    <!--code to trim the string to 100 chars and readMore var-->
    <p> <%= pos[i].content.substring(0, 99) + " ... "; %> <a href="/post/<%=pos[i].text%>">Read More</a> </p>    

<% } %>
<% */ %>


<%- include('partials/footer') %>               <!--to render footer page using ejs-->  